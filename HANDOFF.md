# ASAT — handoff pointer

This file used to carry a living "what to do next" summary; that role
now belongs to two places that are already kept current:

- **What ASAT is, who it serves, the file map** —
  [`README.md`](README.md).
- **What to work on next** — every open gap is a numbered entry in
  [`docs/FEATURE_REQUESTS.md`](docs/FEATURE_REQUESTS.md), each with
  the user-visible problem and an implementation sketch.

If you are starting a fresh Claude Code session: paste the README,
then pick the next feature request you want to land. The developer
guide ([`docs/DEVELOPER_GUIDE.md`](docs/DEVELOPER_GUIDE.md)) lays
out the PR recipe every change follows.
